"use client";

/**
 * Homepage styles component
 * Note: Most styles now use global CSS classes from globals.css:
 * - gradient-mesh (background)
 * - glass-effect (card)
 * - text-gradient (title)
 * - Animation utilities (animate-in, slide-in, scale-in)
 */
export function HomePageStyles() {
  // All styles are now in globals.css for consistency across auth pages
  return null;
}
